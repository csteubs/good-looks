/** The Playwright reporter, as a raw JS string written beside the specs. It is
 *  loaded by the Playwright CLI, not by anything in this repo — see
 *  `check:step-progress` for why that matters. */
export declare const stepReporterSource: string;
